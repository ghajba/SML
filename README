This repository contains Standard ML files which are some simple (and later on more difficult) tests and programs I've created during getting in touch with the language.

I do not use EMACS for the development, Notepad++ seems just fine (it has a CAML sintax coloring which fits perfectly my needs).